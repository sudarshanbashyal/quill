<!-- The circle behind the user's initial on the Profile screen. Tonal brand fill, no stroke —
     it's an identity marker, not a control, so it shouldn't read as a tappable button. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/brand_purple_light" />
</shape>
