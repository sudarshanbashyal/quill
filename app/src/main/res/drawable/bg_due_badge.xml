<?xml version="1.0" encoding="utf-8"?>
<!-- Pill behind a deck's due count. Tinted in code — brand purple when there's something to
     review, a flat grey when there isn't. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/deck_badge_radius" />
    <solid android:color="@color/brand_purple" />
</shape>
