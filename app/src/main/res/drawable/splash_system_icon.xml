<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately empty: the icon slot on Android 12's system splash, drawing nothing. See the
     windowSplashScreenAnimatedIcon comment in values/themes.xml for why it isn't the logo.
     A drawable rather than @android:color/transparent because the platform wants something with
     intrinsic bounds to fit into the icon slot. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0h108v108h-108z" />
</vector>
