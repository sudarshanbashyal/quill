<?xml version="1.0" encoding="utf-8"?>
<!-- One card at a time, filling the screen. Nothing else competes with it: the header is a back
     arrow and a position, and the two grading buttons only appear once the answer is showing, so
     there is never a choice to make before the question has actually been thought about. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    tools:context=".ui.flashcards.FlashcardsFragment">

    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/spacing_md"
        android:paddingTop="@dimen/spacing_md"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/back_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:padding="@dimen/spacing_sm"
            android:text="←"
            android:textColor="@color/text_primary"
            android:textSize="22sp" />

        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/spacing_xs"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/flashcards_title"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/card_position"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_sm"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            tools:text="2 / 8" />

        <!-- Deleting the deck belongs here as well as on the decks list: this is where you find out
             a deck is stale, and being sent to another screen to act on that is the kind of detour
             that leaves it undeleted. -->
        <ImageView
            android:id="@+id/delete_deck_button"
            android:layout_width="@dimen/deck_delete_size"
            android:layout_height="@dimen/deck_delete_size"
            android:layout_marginStart="@dimen/spacing_sm"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:contentDescription="@string/action_delete_flashcards"
            android:focusable="true"
            android:padding="@dimen/spacing_sm"
            android:src="@drawable/ic_clear"
            app:tint="@color/text_secondary" />

    </LinearLayout>

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_sm"
        android:layout_marginHorizontal="@dimen/spacing_md"
        app:trackCornerRadius="4dp"
        app:trackThickness="4dp"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- The card. Tapping anywhere on it turns it over — the whole surface is the affordance, so
         there's no button to aim at while thinking. -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="@dimen/spacing_md"
        android:clickable="true"
        android:focusable="true"
        android:minHeight="@dimen/flashcard_min_height"
        app:cardBackgroundColor="@color/surface_container"
        app:cardCornerRadius="@dimen/flashcard_corner_radius"
        app:cardElevation="0dp"
        app:layout_constraintTop_toBottomOf="@id/progress"
        app:layout_constraintBottom_toTopOf="@id/grading_row"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="@dimen/spacing_lg">

            <TextView
                android:id="@+id/card_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:letterSpacing="0.12"
                android:text="@string/flashcard_label_question"
                android:textColor="@color/text_secondary"
                android:textSize="12sp"
                android:textStyle="bold" />

            <ScrollView
                android:id="@+id/card_scroll"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:fillViewport="true"
                android:scrollbars="none">

                <TextView
                    android:id="@+id/card_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:gravity="center"
                    android:paddingVertical="@dimen/spacing_md"
                    android:textColor="@color/text_primary"
                    android:textSize="20sp"
                    tools:text="Which country has the most time zones?" />

            </ScrollView>

            <TextView
                android:id="@+id/card_hint"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/flashcard_tap_to_reveal"
                android:textColor="@color/text_secondary"
                android:textSize="13sp" />

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- Held INVISIBLE rather than GONE until the answer is showing, so the card doesn't resize
         underneath the user at the moment they're reading it. -->
    <LinearLayout
        android:id="@+id/grading_row"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:paddingBottom="@dimen/spacing_lg"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- The zeroed padding and insets are what actually centre the glyph. A MaterialButton
             positions its icon relative to the *text* block and then insets its background
             vertically (6dp top and bottom by default), so at a fixed square size the fill comes out
             as a short ellipse with the icon sitting up and to the start of it. With no text there
             is nothing for those allowances to make room for. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_incorrect"
            style="@style/Widget.Material3.Button.IconButton.Filled.Tonal"
            android:layout_width="@dimen/flashcard_grade_button_size"
            android:layout_height="@dimen/flashcard_grade_button_size"
            android:layout_marginEnd="@dimen/spacing_lg"
            android:contentDescription="@string/action_answer_incorrect"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:padding="0dp"
            app:backgroundTint="@color/answer_incorrect_container"
            app:cornerRadius="@dimen/flashcard_grade_button_radius"
            app:icon="@drawable/ic_incorrect"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="@dimen/flashcard_grade_icon_size"
            app:iconTint="@color/answer_incorrect" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_correct"
            style="@style/Widget.Material3.Button.IconButton.Filled.Tonal"
            android:layout_width="@dimen/flashcard_grade_button_size"
            android:layout_height="@dimen/flashcard_grade_button_size"
            android:layout_marginStart="@dimen/spacing_lg"
            android:contentDescription="@string/action_answer_correct"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:padding="0dp"
            app:backgroundTint="@color/answer_correct_container"
            app:cornerRadius="@dimen/flashcard_grade_button_radius"
            app:icon="@drawable/ic_correct"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="@dimen/flashcard_grade_icon_size"
            app:iconTint="@color/answer_correct" />

    </LinearLayout>

    <!-- Covers the review area for every state that isn't a card: the end-of-session summary, a
         deck with nothing due, and a note whose Q&A blocks have since gone. One panel, because all
         three are the same shape — a headline, a line of detail, and a way onward. -->
    <LinearLayout
        android:id="@+id/message_panel"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@color/app_background"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingHorizontal="@dimen/spacing_lg"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/progress"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/message_icon"
            android:layout_width="@dimen/flashcard_message_icon_size"
            android:layout_height="@dimen/flashcard_message_icon_size"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_flashcard"
            app:tint="@color/brand_purple" />

        <TextView
            android:id="@+id/message_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:gravity="center"
            android:textColor="@color/text_primary"
            android:textSize="20sp"
            android:textStyle="bold"
            tools:text="Session complete" />

        <TextView
            android:id="@+id/message_body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:gravity="center"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            tools:text="7 of 8 right first time." />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/message_primary_button"
            style="@style/Widget.Material3.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_lg"
            tools:text="Review again" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/message_secondary_button"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            tools:text="Back to note" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
