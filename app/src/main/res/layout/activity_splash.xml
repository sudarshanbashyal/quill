<?xml version="1.0" encoding="utf-8"?>
<!-- The background is repeated from Theme.Quill.Splash's windowBackground so the fill still
     reaches under the system bars once the platform forces edge-to-edge (API 35+), where the
     statusBarColor in the theme is ignored. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/splash_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/splash_background">

    <mse.quill.ui.splash.QuillLogoView
        android:id="@+id/splash_logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/app_name"
        android:textColor="@color/brand_ink"
        android:textSize="@dimen/splash_logo_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
